# Fullstack User CRUD – Frontend

This is the React frontend for the Fullstack User CRUD application.

It provides authentication (login/register) and a protected admin dashboard.

Features:
- JWT-based authentication (HTTP-only cookies)
- Protected routes
- Full CRUD operations for users
- Pagination support
- Cypress + Jest tested

Built with React, Vite, and modern best practices.